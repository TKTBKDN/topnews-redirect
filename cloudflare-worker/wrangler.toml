# Cloudflare Workers Configuration
# Deploy via: Cloudflare Dashboard -> Workers & Pages -> Create -> Connect to Git

name = "topnews-redirect"
main = "worker.js"
compatibility_date = "2024-01-01"

# Enable workers.dev subdomain
workers_dev = true

# KV Namespace - Thêm sau khi deploy thành công
# Vào Dashboard -> Workers -> topnews-redirect -> Settings -> Variables -> KV Namespace Bindings
# Binding name: META_CACHE
